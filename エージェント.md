# エージェント向けメモ

## プロジェクト概要

このフォルダは、HTML/CSS/JavaScriptだけで動く在庫管理アプリです。

- Node.js、npm、データベース、外部CDNは使わない
- `index.html` をブラウザで直接開いて使う
- データはブラウザの `localStorage` に保存する
- PC間やブラウザ間のデータ移動はJSON出力/読込で行う

## 対象ファイル

- `index.html`: 画面構造
- `styles.css`: 画面デザイン
- `app.js`: 在庫管理ロジック
- `エージェント.md`: このメモ

## 注意事項

- この `aeon` フォルダだけをGit管理対象にする
- 親フォルダや他プロジェクトのファイルをコミットに含めない
- 会社PCで追加インストールが必要になる構成へ変更しない
- データ自動同期が必要な場合でも、HTML単体の制限を超える変更は事前に確認する

## 動作確認

1. `index.html` をブラウザで開く
2. 商品を登録する
3. 入庫または出庫を行う
4. JSON出力/読込が動くことを確認する
